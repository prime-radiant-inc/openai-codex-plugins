interface:
  display_name: "Verification Before Completion"
  short_description: "Require fresh verification evidence before claiming any work is done"
  default_prompt: "Use $verification-before-completion before claiming a task is complete, fixed, or passing — evidence before assertions, always."
