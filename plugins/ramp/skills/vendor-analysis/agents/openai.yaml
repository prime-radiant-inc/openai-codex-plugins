interface:
  display_name: "Vendor Analysis"
  short_description: "Analyze vendor spend and ownership data"
  icon_small: "../../../assets/app-icon.png"
  icon_large: "../../../assets/app-icon.png"
  default_prompt: "Use Ramp to analyze vendor spend, identify top vendors, and summarize ownership and renewal risk."

dependencies:
  tools:
    - type: "mcp"
      value: "ramp"
      description: "Ramp MCP server"
      transport: "streamable_http"
      url: "https://mcp.ramp.com/mcp"
